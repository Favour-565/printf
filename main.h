#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>

int my_putchar(char c);
int put_str(char *str);
int myPuts(char *str);
int put_int(int x);
int _printf(const char *format, ...);


#endif
